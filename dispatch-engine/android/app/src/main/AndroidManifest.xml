<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Obrigatória a partir do Android 13 (API 33) pra qualquer notificação
         aparecer — sem ela, PushNotifications.requestPermissions() (chamado em
         registrarPushEntregador(), app-entregador.html) não tem efeito real.
         targetSdkVersion=36 aqui, então é obrigatória, não opcional. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- item 39 (25/08/2026): achado ao vivo — iniciarRastreioPosicao() já
         existia (app-entregador.html) usando navigator.geolocation puro, mas
         SEM essas 2 permissões o WebView nega a geolocalização sempre, sem
         nem mostrar prompt — minhaPosicaoAtual nunca era preenchida no app
         nativo (só funcionava no navegador/mockup, onde o Chrome pede a
         permissão por conta própria). Faltava tanto isso quanto o plugin
         @capacitor/geolocation (ver carregarEntregador()/iniciarRastreioPosicao()),
         que é quem de fato dispara o prompt nativo de runtime usando essas
         permissões declarativas. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
</manifest>
